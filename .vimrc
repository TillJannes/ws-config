" user config: line numbers, relative line numbers, cursor line, indentation, tabstop
set number
set cursorline
set relativenumber
set autoindent
set tabstop=2

" show mode on last line of screen
set showmode

" turn syntax highlighting on, set color theme and set colors to actually work
syntax on
set termguicolors
colorscheme catppuccin_mocha
